MEDIA SLOTS — drop files here with these exact names
====================================================

Add ?slots to the page URL (e.g. index.html?slots) to see every slot
outlined in orange with its file name.

1. hero-portrait.png        (recommended: transparent PNG cut-out, ~1600px tall)
   Shows in the hero, inside the ring, in front of the dotted "J CUT".
   Until it exists, a morphing orange blob with the J logo is shown instead.

2. showreel.mp4             (recommended: 16:9, H.264, under ~15 MB)
   showreel-poster.jpg      (a still frame shown before playing)
   Appears in a glass frame under the "We build the system that keeps you
   Posting" statement. Click to play with sound. If the file is missing,
   this whole block stays hidden.

Team photos already live in assets/img/team/ (same as the current site).
